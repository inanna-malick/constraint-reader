main :: IO ()
main = putStrLn "Test suite not yet implemented"


{-
need internet to grab hspec and build tests, so let's just sketch them out:
- test for service thingy that only uses pure (monad state, monad writer) stack
- test that writes some stuff, etc, etc, then, idk, asserts on what's written
-}
